<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.wxz.core.dao.product.BrandMapper">
	<!-- resultMap -->
	<resultMap type="edu.wxz.core.bean.product.Brand" id="brand">
		<!-- 
			<id/>唯一标识查询结果集
		-->
		<result column="id" property="id"/>
		<result column="name" property="name"/>
		<result column="description" property="description"/>
		<result column="img_url" property="imgUrl"/>
		<result column="sort" property="sort"/>
		<result column="is_display" property="isDisplay"/>
	</resultMap>

	<!-- 查询品牌-->
	<select id="getBrandListWithPage" parameterType="edu.wxz.core.bean.product.Brand" resultMap="brand">
		SELECT id,name,description,img_url,sort,is_display
		FROM bbs_brand
		<where>
			<if test="isDisplay != null">
				is_display = #{isDisplay}
			</if>
			<if test="name != null"> 
				and name = #{name}
			</if>
		</where>
		ORDER BY id DESC
		LIMIT #{startRow},#{pageSize}
	</select>
	
	<!-- 查询总记录数 -->
	<select id="getBrandCount" parameterType="edu.wxz.core.bean.product.Brand" resultType="java.lang.Integer">
		SELECT COUNT(1)
		FROM bbs_brand
		<where>
			<if test="isDisplay != null">
				is_display = #{isDisplay}
			</if>
			<if test="name != null">
				and name = #{name}
			</if>
		</where>
	</select>
	
	<!-- 添加品牌 -->
	<insert id="addBrand" parameterType="edu.wxz.core.bean.product.Brand">
		INSERT INTO bbs_brand
		<trim prefix="(" suffix=")">
			name,
			description,
			img_url,
			sort,
			is_display
		</trim>
		values
		<trim prefix="(" suffix=")">
			#{name},
			#{description},
			#{imgUrl},
			#{sort},
			#{isDisplay}
		</trim>
	</insert>
	
	<!-- 通过主键删除品牌 -->
	<delete id="deleteBrandBtKey" parameterType="java.lang.Integer">
		delete from bbs_brand
		<where>
			id = #{id}
		</where>
	</delete>
	
	<!-- 通过主键批量删除品牌 -->
	<delete id="deleteBrandByKeys" parameterType="java.lang.Integer">
		delete from bbs_brand
		<where>
			id in
			<foreach collection="array" item="id" open="(" close=")" separator=",">
				#{id}
			</foreach>
		</where>
	</delete>		
	
	<!-- 修改 -->
	<update id="updateBrandByKey" parameterType="edu.wxz.core.bean.product.Brand">
		update bbs_brand 
		<set>
			<if test="name != null">
				name = #{name}, 
			</if>
			<if test="description != null">
				description = #{description},
			</if>
			<if test="imgUrl != null">
				img_url = #{imgUrl},
			</if>
			<if test="sort != null">
				sort = #{sort},
			</if> 
			<if test="isDisplay != null">
				is_display = #{isDisplay}
			</if>
		</set>
		<where>
			id = #{id}
		</where>
	</update>
	
	<!-- 通过id查询一个品牌对象 -->
	<select id="getBrand" parameterType="java.lang.Integer" resultMap="brand">
		SELECT id,name,description,img_url,sort,is_display
		FROM bbs_brand
		<where>
			id = #{id}
		</where>
	</select>
</mapper>